// Overwritten by the container entrypoint at startup. The default below works
// when a reverse proxy serves the API on the same origin under /api.
window.__safa_HAWA__ = {
  apiBaseUrl: "/api/v1",
};
