[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = [
    "E",
    "F",
    "I",
    "B",
    "UP",
]
ignore = [
    "B008",  # FastAPI Depends pattern in function parameter defaults
]

[tool.pytest.ini_options]
testpaths = ["api/tests", "poller/tests"]
